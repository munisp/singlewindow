# NCS–NRS Gateway

> **Status (Phase 20 orphan-code audit):** Planned — NOT DEPLOYED. NCS↔NRS integration currently lives in the monorepo (`ncsNrs` router, /app/ncs-nrs page). Not referenced by docker-compose or CI.
