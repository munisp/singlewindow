# Manifest Service

> **Status (Phase 20 orphan-code audit):** Planned — NOT DEPLOYED. Duplicate of the in-monorepo manifest functionality (tRPC routers + /app/manifests page). Not referenced by docker-compose or CI.
