# UCR Service

> **Status (Phase 20 orphan-code audit):** Planned — NOT DEPLOYED. Duplicate of the in-monorepo UCR functionality (tRPC `ucr` router + /app/ucr page). No docker-compose service, no CI workflow, no importer. Retained as reference implementation.
