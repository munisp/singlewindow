[package]
name = "tradegateway-rust-benchmark"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "benchmark-server"
path = "src/main.rs"

[dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
