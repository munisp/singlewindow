apiVersion: 1

providers:
  - name: "TradeGateway Dashboards"
    orgId: 1
    folder: "TradeGateway™ NGSWTP"
    folderUid: "tradegateway"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false
