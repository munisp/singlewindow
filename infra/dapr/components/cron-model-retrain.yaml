apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: cron-model-retrain
  namespace: tradegateway
spec:
  type: bindings.cron
  version: v1
  metadata:
  - name: schedule
    value: "0 2 * * 0"  # Every Sunday at 02:00 UTC — weekly model retraining
