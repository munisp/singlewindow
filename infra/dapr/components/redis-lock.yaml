apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: redis-lock
  namespace: tradegateway
spec:
  type: lock.redis
  version: v1
  metadata:
  - name: redisHost
    value: "redis:6379"
  - name: redisPassword
    secretKeyRef:
      name: redis-secret
      key: password
